{
  "name": "tinylocal",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "pnpm run wasm:build && pnpm run wasm:opt && vite build",
    "preview": "vite preview",
    "wasm:build": "wasm-pack build rust --target web --out-dir ../web/pkg --release",
    "wasm:opt": "bash scripts/optimize-wasm.sh"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
